CREATE TABLE StudentEnrollments (
    enrollment_id INT PRIMARY KEY,
    student_name VARCHAR(100) NOT NULL,
    course_id VARCHAR(10) NOT NULL,
    enrollment_date DATE NOT NULL,
    CONSTRAINT unique_student_course UNIQUE(student_name, course_id)
);

BEGIN;
INSERT INTO StudentEnrollments (enrollment_id, student_name, course_id, enrollment_date) VALUES (1, 'Ashish', 'CSE101', DATE '2024-07-01');
COMMIT;
BEGIN;
INSERT INTO StudentEnrollments (enrollment_id, student_name, course_id, enrollment_date) VALUES (2, 'Ashish', 'CSE101', DATE '2024-07-01');
ROLLBACK;
SELECT * FROM StudentEnrollments;

BEGIN;
SELECT * FROM StudentEnrollments WHERE student_name = 'Ashish' AND course_id = 'CSE101' FOR UPDATE;

BEGIN;
UPDATE StudentEnrollments SET enrollment_date = DATE '2024-07-02' WHERE student_name = 'Ashish' AND course_id = 'CSE101';

COMMIT;
SELECT * FROM StudentEnrollments;

BEGIN;
SELECT * FROM StudentEnrollments WHERE enrollment_id = 1 FOR UPDATE;

BEGIN;
UPDATE StudentEnrollments SET enrollment_date = DATE '2024-07-03' WHERE enrollment_id = 1;

UPDATE StudentEnrollments SET enrollment_date = DATE '2024-07-02' WHERE enrollment_id = 1;
COMMIT;

COMMIT;
SELECT * FROM StudentEnrollments;
